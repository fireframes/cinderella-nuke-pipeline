import os
import nuke

# NOTE: The main initialization for gizmo directories is now handled in the root init.py
# This file is kept for compatibility, but doesn't add paths to avoid duplication
# which was causing path issues with double backslashes.
